sudo: required
dist: trusty
language: ruby
rvm:
  - 2.3.1
jdk:
  - openjdk8
node_js:
  - node
services:
  - redis
before_install:
  - node -v
  - npm -v
  - npm install --global gulp-cli
  - npm install
script:
  - gulp
  - bundle exec jekyll build
notifications:
  email: false